oneup_flysystem:
    adapters:
        catalog_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/storage/catalog'
        jobs_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/storage/jobs'
        archivist_adapter:
            local:
                location: '%kernel.project_dir%/var/storage/archive'
        category_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/storage/category'
        local_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/storage/local'
